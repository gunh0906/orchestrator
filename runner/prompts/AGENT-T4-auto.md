You are executing task AGENT-T4 (Role: Frontend).

## Goal: IGES 면적 계산기 웹 프론트엔드 구현. index.html: 파일 업로드 드래그앤드롭 영역, 결과 표시 패널, Three.js 3D 뷰어 영역. app.js: 파일 업로드 fetch, 결과 렌더링 (총면적/외곽면적/내부면적/전개면적 표시), 면별 상세 테이블. style.css: 깔끔한 다크테마 UI. CDN으로 Three.js 로드. 한국어 UI.

### Scope
Files to work on:
- static/index.html
- static/app.js
- static/style.css

### Instructions
1. Read each file in scope completely
2. Understand the codebase context around these files
3. Execute the goal described above
4. Verify syntax after editing (python -c or node -c)
5. Print a summary of changes at the end

### Constraints
- ONLY modify files listed in scope
- Verify syntax after editing
