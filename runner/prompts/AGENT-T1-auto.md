You are executing task AGENT-T1 (Role: Backend-Server).

## Goal: FastAPI 서버 구현. 엔드포인트: POST /api/upload (IGES 파일 업로드), GET /api/result/{id} (계산 결과), GET / (정적 파일 서빙). iges_parser.py와 surface_calc.py를 import하여 사용. 업로드된 파일을 임시 저장 후 파싱+계산 파이프라인 실행. CORS 허용.

### Scope
Files to work on:
- server.py
- requirements.txt

### Instructions
1. Read each file in scope completely
2. Understand the codebase context around these files
3. Execute the goal described above
4. Verify syntax after editing (python -c or node -c)
5. Print a summary of changes at the end

### Constraints
- ONLY modify files listed in scope
- Verify syntax after editing
