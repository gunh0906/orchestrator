You are executing task AGENT-T3 (Role: Backend-Calc).

## Goal: OpenCASCADE 기반 내부 형상 면적 계산 모듈 구현. calculate_face_area(face) 함수: BRepGProp으로 개별 면 면적 계산. calculate_unfolded_area(internal_faces) 함수: 내부 면들을 평면 전개(unfold)했을 때 총 면적 산출. get_summary(shape) 함수: iges_parser를 사용하여 전체 결과 딕셔너리 반환 (total_area, outer_area, internal_area, unfolded_area, face_details 리스트). numpy 사용 가능.

### Scope
Files to work on:
- surface_calc.py

### Instructions
1. Read each file in scope completely
2. Understand the codebase context around these files
3. Execute the goal described above
4. Verify syntax after editing (python -c or node -c)
5. Print a summary of changes at the end

### Constraints
- ONLY modify files listed in scope
- Verify syntax after editing
