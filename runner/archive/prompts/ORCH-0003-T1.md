You are executing task {{TASK_ID}}.

Owner: {{OWNER}}
Repo: {{REPO}}
Scope paths (do not edit outside):
{{SCOPE_PATHS}}

Goal:
{{GOAL}}

Done when:
{{DONE_WHEN}}

Execution constraints:
- Keep UTF-8.
- Preserve existing API compatibility (`/api/start`, `/api/stop`, `/api/runs`).
- Update `orchestrator/results.md` section `{{TASK_ID}}`.
